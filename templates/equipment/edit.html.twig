{% extends 'base.html.twig' %}

{% block title %}Editer l'équipement{% endblock %}

{% block body %}
    <h1>Editer l'équipement</h1>

    {{ include('equipment/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('equipment_index') }}">Retour à la liste</a>

    {{ include('equipment/_delete_form.html.twig') }}
{% endblock %}
